import 'package:flutter/cupertino.dart';

class SearchContainer extends StatelessWidget {

  SearchContainer(String name, Str)

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
