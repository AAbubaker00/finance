import 'package:flutter/cupertino.dart';

class SearchContainer extends StatelessWidget {

  SearchContainer(String name, String ma)

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
