import 'package:flutter/cupertino.dart';

class SearchContainer extends StatelessWidget {

  SearchContainer(String name, String market)

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
