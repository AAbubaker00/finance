import 'package:flutter/cupertino.dart';

class SearchContainer extends StatelessWidget {
  String name, market, symbol;
  SearchContainer({this.s});

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
