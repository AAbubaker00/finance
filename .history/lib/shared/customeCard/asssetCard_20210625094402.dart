import 'package:flutter/material.dart';

class CustomeAssetCard extends StatelessWidget {
  const CustomeAssetCard({Key?? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}